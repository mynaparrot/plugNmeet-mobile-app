name: pnm_flutter_demo
description: plugNmeet hybrid integration demo (native publisher + web subscriber)
version: 0.0.1

environment:
  sdk: ^3.0.0
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.8.0
  webview_flutter: ^4.10.0
  http: ^1.2.0
  flutter_background: ^1.2.0
  flutter_webrtc: ^1.5.2
  permission_handler: ^11.3.0
  crypto: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
